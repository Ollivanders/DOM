#!/bin/sh
#
# dot
#
# dot` handles installation, updates, things like that. Run it periodically
# to make sure you're on the latest and greatest.
# TODO: run letsgo, config file version for onky updates and installations, without full on user interface

set -eu

~/.dotfiles/letsgo.sh "$@"
